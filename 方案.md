# agent-nexus `conf` 命令重构方案

> 目标：把 `conf set` 打造成唯一配置入口，围绕"可恢复"构建快照体系，同时
> 修复当前已知的架构缺陷。

---

## 一、需求梳理（用户 7 条 → 技术语义）

| # | 原话 | 技术含义 |
|---|------|----------|
| 1 | `conf set` 是唯一设置配置的命令 | 写入 proxy 配置的入口唯一；已删除 `conf auto`（原本走另一套 pipeline 且已坏） |
| 2 | 每次设置配置后，可恢复 | 每次写入前自动拍 **pre-write 快照** |
| 3 | 多次配置后，能恢复到"原生配置"（agent-nexus 引入之前用户手动的） | 每个 agent 首次被触碰时拍 **baseline 快照**，`--reset` 默认回到它 |
| 4 | 每次设置后，可以恢复到上一次 | pre-write 快照串成时间线，`--reset latest` 恢复上一个 |
| 5 | 无论一个 agent 还是所有 agent，都要快照 | 快照粒度 = 本次操作的 agent 集合 |
| 6 | 知道每个快照的区别，可恢复任意快照 | `conf list` 展示 + `--reset <id>` 精准恢复 |
| 7 | 后来安装的 agent 怎么办 | 首次触碰时按 `HasConfig` 自动判断：无既有配置则跳过 baseline，有则先拍 baseline 再写 |

---

## 二、核心发现：两类 writer 的 reset 语义不同

通读了 8 个 writer 的 `Reset()` 实现：

### Delete 型（hermes / kimi / gemini）
- `Configure()` 整文件覆写（`os.WriteFile` 覆盖全部）
- `Reset()` 直接删文件
- **有损**：必须靠 baseline 快照才能恢复用户手动配过的内容

### Merge 型（claude / codex / opencode / openclaw / openclaude）
- `Configure()` 只改自己拥有的 key / 段落
- `Reset()` 精准删除自己写入的 key，**保留用户其他配置**
- **无损**：理论上能回到 pre-agent-nexus 状态，无需 baseline

但即便 merge 型"无损"，用户仍可能在**两次 conf set 之间手动修改配置**，所以：
- **pre-write 快照对所有 writer 都必要**（满足需求 2、4、5）
- **baseline 对 delete 型必需**，对 merge 型是兜底（满足需求 3）

---

## 三、快照分类

统一写入现有 `backup_snapshots` / `backup_config_entries` 表，用 `type` 字段区分：

| type | 触发时机 | 命名规则 | 对应需求 |
|------|---------|---------|---------|
| `baseline` | 某 agent **首次**被 `conf set` 触碰，且 `HasConfig=true, IsConfigured=false` | `baseline:<agent>:<YYYY-MM-DD_HH-MM-SS>` | 3 |
| `pre-write` | 每次 `conf set` 写入前（对操作集合中的所有 agent） | `pre-write:<agents>:<timestamp>` | 2、4、5 |
| `pre-reset` | 每次 `conf set --reset` 执行前 | `pre-reset:<agents>:<timestamp>` | 安全网 |
| `manual` | `conf backup` 手动触发 | 用户自定 | 显式保存 |

### baseline 触发条件

```
agent.HasConfig == true   // 配置文件存在（说明 agent 已装且可能用户手动配过）
&& agent.IsConfigured == false   // 文件里没有 agent-nexus 的 proxy 标记
&& 该 agent 尚不存在 type=baseline 的快照   // 避免重复
→ 拍 baseline
```

`HasConfig == false`（agent 刚装完，配置文件都还没有）→ **跳过 baseline**，直接写入。

---

## 四、`--reset` 语义分层

```
conf set --agent opencode --db 1                        # 写入配置（正常路径）
conf set --agent opencode --reset                       # 恢复该 agent 的 baseline（默认）
conf set --agent opencode --reset baseline              # 显式指定 baseline
conf set --agent opencode --reset latest                # 恢复该 agent 上一个 pre-write
conf set --agent opencode --reset <snapshot-id>         # 恢复任意指定快照
conf set --agent opencode --reset <snapshot-name>       # 按名称恢复
conf set --agent all --reset                            # 所有 agent 回到各自 baseline
conf set --agent opencode --dry-run                     # 预览写入，不实际写入
conf set --agent opencode --dry-run --reset             # 预览恢复到 baseline（当前缺失）
```

### `--reset` 不带 id 的默认行为

1. 优先找该 agent 的 `type=baseline` 快照（按 `created_at` 最新）
2. 若 baseline 不存在，找该 agent 最早的 `type=pre-write` 快照（兜底）
3. 若都不存在，打印提示"该 agent 尚未被 agent-nexus 配置过"，跳过

---

## 五、后来安装的 agent 处理（需求 7）

**场景**：先装了 codex/claude 并 conf set 了几次，后来装了 opencode。

`conf set --agent all --db 1` 时：
1. `resolveAgentList("all")` 自动把 opencode 加进来
2. 对 opencode 走正常写入路径，写入前检查 `HasConfig`：
   - `false`（刚装完，无配置文件）→ 跳过 baseline，直接写入
   - `true && IsConfigured==false`（装了但用户手动配过）→ **先拍 baseline，再写入**

**不需要用户决策**，完全自动。

---

## 六、现有缺陷及处理

### P0 — 已修复 ✅

`MarkFlagRequired("db")` 在 cobra 阶段拦截，导致 `conf set --reset` 必须带 `--db`。
已改为在 `RunE` 中条件校验：仅 `!opts.reset` 时检查 `--db` 非空。

### P1 — 三份重复的快照创建代码

- `runconfset.go:515` `createAutoSnapshot`
- `runconfbackup.go:81` `runConfBackup`
- `runconfrestore.go:240` `createPreRestoreSnapshot`

**处理**：抽共享函数 `takeSnapshot(agents, opts) SnapshotRef`，放新文件
`cmd/conf_snapshot.go`，三处调用点统一。

### P1 — `conf set` 同时做 set 和 reset，参数语义冲突

- `--db` 对 set 核心，对 reset 无意义
- `--dry-run` 在 reset 下**完全未实现**（`runConfReset` 没有任何 dry-run 判断）
- `--backup-name` 在两条路径下含义不同

**处理**：保留 `--reset` 作为 set 的子模式（`conf set` 仍是唯一入口），但：
- reset 流程彻底独立到 `runConfReset`
- 补 `--dry-run` 在 reset 下的实现
- `--backup-name` 在 reset 下只给 pre-reset 快照命名

### P2 — ~~`conf auto` 是坏的薄包装~~ 已删除 ✅

`RunE` 里没传 `db`/`proxy` 给 pipeline，直接掉进 `resolveDBArg("")` 报错。

**处理**：直接删除（2026-08-09）——从 `root.go` 移除注册，`runconfauto.go` 和
`runconfauto_test.go` 保留空桩（沙箱阻止删除 git-tracked 文件）。文档中不再出现
`conf auto` 的用法，改用 `conf set --agent all --db auto`。

### P2 — `confCmd.Long` help 文本与实际不符

`root.go:977` 只列了 4 个子命令，却引用了 `history/rollback/diff/branch`
这些不存在的命令。

**处理**：重写 help，只列真实存在的 `set/list/restore/backup/models`，
加上 `--reset` 子模式说明。

### P2 — `resolveDBArg` 哨兵值混用

`"auto"` → `0`，空/无效 → `-1`。`0` 和 `-1` 都是"非正常 id"但含义不同。

**处理**：重构为 `dbRef` 结构体 `{Mode: "auto"|"id"; ID: int}`，失败走
`error`，不用魔法数字当哨兵。

**状态：已完成（2026-08-09）**。`dbRef` 定义于 `cmd/conf_utils.go`；
`runConfSetFromDB` 按 `dbR.Mode=="auto"` 与 else 分支分别走 `GetMinIDProxy` /
`GetByID`，彻底消除 `0`=auto、`-1`=error 的哨兵值歧义。

### P3 — `runconfset.go` 660 行过大

**处理**：拆分：
- `cmd/conf_set.go` — `conf set` 命令定义 + `runConfSetFromDB`
- `cmd/conf_reset.go` — `runConfReset`
- `cmd/conf_snapshot.go` — 共享的 `takeSnapshot` + `baselineIfNeeded`
- `cmd/conf_utils.go` — `resolveAgentList` / `resolveDBArg` / `parseModelsStr` / `idsList` / `getProxySource`

---

## 七、数据库 schema

**不需要改。** 复用现有 `backup_snapshots.type` 字段承载四种 phase。
`agent_name` 字段记录本次操作涉及的 agent（多个用逗号分隔或 `ALL`）。

若后续需要更严谨，可新增 `phase TEXT` 列并加 migration，但第一阶段不必须。

---

## 八、实施顺序

| 步骤 | 内容 | 影响范围 |
|------|------|---------|
| 1 | ~~P0 修复~~ ✅ | `runconfset.go` |
| 2 | 抽 `takeSnapshot` 共享函数 | 新增 `cmd/conf_snapshot.go`，改 3 个调用点 |
| 3 | 实现 `baselineIfNeeded` | `conf_snapshot.go` 新增 |
| 4 | 重构 `runConfReset`：支持 `--reset [baseline|<id>|latest]`，补 `--dry-run` | `conf_reset.go` 新增 |
| 5 | 删除 `conf auto` | `runconfauto.go` 空桩、删测试、从 root.go 移除注册 | ✅ |
| 6 | 修 `confCmd.Long` help 文本 | `root.go` |
| 7 | 重构 `resolveDBArg` → `dbRef` 结构体 | `conf_utils.go` |
| 8 | 拆分 `runconfset.go`（可选） | 新增 3 个文件，迁移代码 |
| 9 | 更新 E2E 测试 + 文档 | 测试文件 |

---

## 九、决策点（需要你确认）

1. **`--reset` 不带 id 的默认行为**：回 baseline（推荐）还是回上一个 pre-write？
2. **baseline 触发时机**：`HasConfig && !IsConfigured` 拍 baseline（推荐），还是每次都拍？
3. **snapshot type 字段**：复用 `type` 存 phase（推荐，最小改动）还是新增 `phase` 列？
4. **~~`conf auto`~~**：**已删除**（2026-08-09），文档中不再出现。